# 术语词典

`terminology/*.yaml` 共享版本号。每个条目包含稳定 ID、标准中文、别名、语义种类、Lean
模板、优先级、示例和说明。规范化采用最长匹配，并记录每次映射。`terminology check`
检测跨文件别名冲突与版本不一致。

用户扩展时应新建 YAML 文件并沿用当前版本；同一别名不能属于两个 ID。词典只负责候选
语义和局部规范化，完整语义仍由解析器根据语法结构决定，不能只靠字符串替换。

