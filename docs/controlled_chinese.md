# 受控中文规范

结构化格式优先，章节为“定理名称、变量、假设、结论、证明”。变量必须写成 `x：实数`
或 `x 是实数`，支持实数、自然数、整数、有理数。假设可以写 `h1：x > 0`；未命名假设
按顺序获得 `h1`、`h2`。表达式支持括号、函数应用、`+ - * / ^`、比较、且/或/否定以及
成员/包含关系。

有限自然句式为：`对任意实数 x，如果 x > 0，那么 x + 1 > 0。`。缺失类型、未声明变量、
量词范围不清或未知符号会产生结构化警告，不会被猜测。自由散文证明、隐含变量与依赖语境
的代词目前不受支持。

